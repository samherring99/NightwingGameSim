# out/

Output directory for compiled GameBoy ROM files (.gb).

Successfully compiled games are stored here with naming convention:
- Single generations: `game.gb`, `output.gb`, or custom name
- Batch evaluations: `{prompt_id}_{url_index}.gb`

These .gb files can be played in any GameBoy emulator (BGB, mGBA, SameBoy, etc.)
